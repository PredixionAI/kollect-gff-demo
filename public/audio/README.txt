This folder is no longer used.

The voice-picker preview ("Replay" on each orb) now speaks a sample line
in-browser via the Web Speech API instead of playing a recorded clip — see
sampleText in server/voiceCatalog.js and public/js/orbs.js. No audio files
need to be dropped here.
